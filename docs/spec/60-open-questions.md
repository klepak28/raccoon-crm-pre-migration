# Open Questions

## Scheduler
- `OPEN_QUESTION`: What do the unresolved two compact toolbar buttons next to the date controls do?
- `OPEN_QUESTION`: What is the full behavior of the `Find a time` tab?
- `OPEN_QUESTION`: Does `Undo Schedule` remove only scheduled time, or also clear assignment?
- `OPEN_QUESTION`: What is the exact full option list and ordering inside the live schedule-modal recurrence controls?
- `OPEN_QUESTION`: Does the event composer support recurrence in any hidden or secondary flow?
- `OPEN_QUESTION`: What exact inline edit/delete capabilities exist directly on calendar items versus only through job detail?

## Recurrence
- `OPEN_QUESTION`: Which exact job fields are covered by the note `Job inputs do not carry over on recurring jobs`?
- `OPEN_QUESTION`: Is `Recurring Job New` fully equivalent to starting with a normal job and then enabling recurrence, field by field?
- `OPEN_QUESTION`: Are all ordinal options fully available in monthly/yearly custom recurrence, or only a subset?

## Customers
- `OPEN_QUESTION`: What exact permissions and downstream restrictions are enforced by `Do not service` besides disabling notifications and blocking scheduling?
- `OPEN_QUESTION`: How are multiple addresses prioritized or selected for jobs/invoices?
- `OPEN_QUESTION`: What exact capabilities exist behind `Service Plan` from customer detail?

## Invoices
- `OPEN_QUESTION`: What is the exact field structure of the `Invoice message` modal?
- `OPEN_QUESTION`: What are the full live filters and editable columns in the global `/app/invoices` list?
- `OPEN_QUESTION`: What are the exact live payment-entry steps from invoice preview and invoice list?
- `OPEN_QUESTION`: What exact options appear inside the standalone `Invoice due` modal beyond those observed in `Edit details`?

## Cross-cutting
- `OPEN_QUESTION`: Which behaviors are role-gated by Admin / Office Staff / Field Tech permissions in the tested flows?
- `OPEN_QUESTION`: What exact audit-log coverage exists for all invoice actions, not just the job activity examples captured?
